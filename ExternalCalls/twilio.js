/**
 * Created by mccaul on 12/12/16.
 */
module.exports = {
    sendSMS: function (recipient, myMessage, callback) {
        // console.log("sending to " + recipient + " message " + myMessage);


        var result = 'ok';
        callback(result);
    }
}
